const initialState = {
 logInStatus: 'NOT_LOGGED_IN',
}

const signUpReducer = (state= initialState, {type, payload}) => {
   switch (type) {
    case value:
     
     break;
   
    default:
     break;
   }
}