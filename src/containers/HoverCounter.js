import React from 'react';

const HoverCounter = (props) => (
  <div>
    <h2 onMouseOver={props.incrementCounter} onFocus={this.props.incrementCounter}>ON mouse hover</h2>
  </div>
);

HoverCounter.p

export default HoverCounter;
